import React from 'react';
import PropTypes from 'prop-types';
import ComponentDisplay from 'docs-partials/ComponentDisplay';

/** This is the {{ properCase name }} component. */
class {{ properCase name }}Styleguide extends React.Component {
  render() {
    return (
      <div>
        <h2>{{ properCase name }}</h2>
        <ComponentDisplay
          id="{{ properCase name }}"
          title="{{ properCase name }}"
          component={(() => (
            <div>Test</div>
          ))()
          }
        />
      </div>
    );
  }
}

export default {{ properCase name }}Styleguide;
